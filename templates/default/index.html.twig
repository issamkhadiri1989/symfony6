{% extends 'base.html.twig' %}
{% block body %}
    <p>test test</p>

    {#{% for key, messages in app.flashes(['success']) %}
        <div class="alert alert-{{ key }}">
            {% for message in messages %}
                <p>{{ message }}</p>
            {% endfor %}
        </div>
    {% endfor %}#}
    {{ relative_path(asset('ressources/Cv_test.pdf')) }}<br/>
    {{ absolute_url(asset('ressources/Cv_test.pdf')) }}<br/>

{% endblock %}

{#
{% block javascripts %}
<script type="text/javascript">
    $(document).ready(function () {
        $.ajax({
            type: 'get',
            data : {
                key: 'value'
            },
            url: 'http://localhost/json',
            success: function () {

            }
        });
    });
</script>
{% endblock %}#}
